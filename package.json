{
  "name": "enddesign-ingestor",
  "version": "0.1.0",
  "description": "Design-to-function DAG mapper and agent loop scaffold generator",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ingest": "node src/cli/ingest.js",
    "generate": "node src/cli/generate.js",
    "extract": "node src/cli/extract.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^4.1.2"
  }
}