# NextAura Hybrid Generator

Auto-generated project scaffold created by the NextAura Hybrid Code Generator.

## Project Overview

This project is a hybrid application built with:
- **Frontend**: React 18 + TypeScript + Vite
- **Desktop**: Tauri (Rust backend)
- **Authentication**: OAuth 2.0 with PKCE (RFC 7636)

## Prerequisites

Before you begin, ensure you have the following installed:
- [Node.js](https://nodejs.org/) (v18 or higher)
- [Rust](https://rustup.rs/) (latest stable)
- [pnpm](https://pnpm.io/) or [npm](https://npmjs.com/) (v9 or higher)

## Installation

1. Install dependencies:
   ```bash
   npm install
   # or
   pnpm install
   ```

2. Copy the environment file:
   ```bash
   cp .env.example .env
   ```

3. Update the environment variables in `.env` with your OAuth provider credentials.

## Development

### Run Frontend (Vite)
```bash
npm run dev
```

### Run Tauri Desktop App
```bash
npm run tauri:dev
```

## Building

### Build Frontend
```bash
npm run build
```

### Build Desktop App
```bash
npm run tauri:build
```

## Testing

### Run Tests
```bash
npm run test
```

### Run Tests in Watch Mode
```bash
npm run test:watch
```

### Run Tests with Coverage
```bash
npm run test:coverage
```

## Code Quality

### Lint
```bash
npm run lint
npm run lint:fix
```

### Format
```bash
npm run format
npm run format:check
```

## Project Structure

```
output/
├── src/                    # TypeScript/React frontend
│   ├── components/         # React components
│   ├── oauth/              # OAuth integration
│   └── lib/                # Utility functions
├── src-tauri/              # Rust/Tauri backend
│   ├── src/
│   │   ├── auth/           # Authentication module
│   │   └── main.rs         # Tauri entry point
│   └── Cargo.toml          # Rust dependencies
├── package.json            # Node.js dependencies
├── tsconfig.json           # TypeScript configuration
├── Cargo.toml              # Rust dependencies
└── README.md               # This file
```

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `VITE_OAUTH_CLIENT_ID` | OAuth client ID | - |
| `VITE_OAUTH_CLIENT_SECRET` | OAuth client secret | - |
| `VITE_OAUTH_REDIRECT_URI` | OAuth redirect URI | `http://localhost:1420/callback` |
| `VITE_OAUTH_SCOPE` | OAuth scopes | - |

See `.env.example` for all available options.

## Scripts Reference

| Script | Description |
|--------|-------------|
| `npm run dev` | Start Vite development server |
| `npm run build` | Build for production |
| `npm run preview` | Preview production build |
| `npm run tauri` | Tauri CLI |
| `npm run tauri:dev` | Run Tauri app in development mode |
| `npm run tauri:build` | Build Tauri app for production |
| `npm run test` | Run Vitest tests |
| `npm run lint` | Run ESLint |
| `npm run format` | Format code with Prettier |

## License

MIT License - see LICENSE file for details.

## Generated By

This project was auto-generated by the **NextAura Hybrid Code Generator**.

For more information, visit the [NextAura repository](https://github.com/NextAura).
